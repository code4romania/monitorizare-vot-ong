export interface NgosStateConfig {
    key: string;
    method: string;
    header: string;
    subHeader: string;
}
export let ngosConfig = [{
    key: 'ngos-list',
    method: '',
    header: 'ONG',
    subHeader: '',
}];
